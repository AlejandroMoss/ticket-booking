spring:
  application:
    name: ticket-bookingv3
  datasource:
    url: jdbc:h2:mem:ticketbookingdb
    driver-class-name: org.h2.Driver
    username: moss
    password: alejandromoss05
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
